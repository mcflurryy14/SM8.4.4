<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sm844">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SM844">
        <activity android:name=".PhoneDirectoryActivity"></activity>
        <activity android:name=".AddUserActivity"></activity>
        <activity android:name=".AdministrationActivity"></activity>
        <activity android:name=".MainMenuActivity"></activity>
        <activity android:name=".TasksAndNotificationsActivity"></activity>
        <activity 
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>